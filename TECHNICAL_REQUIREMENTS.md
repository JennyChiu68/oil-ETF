# 原油 ETF 持仓报告上线与每日更新技术需求

文档版本：1.0  
验收日期：2026-07-24  
适用产品：USO（WTI）与 BNO（Brent）原油 ETF 持仓报告

## 一、上线目标

本页面需要公开展示 USO 与 BNO 的最新官方持仓、最新 NAV、总资产净值、期货名义桶数，以及最近五年的历史变化。页面默认按桶展示期货持仓变化，也允许切换为按美元展示官方总资产净值变化。

这里必须把三种数据性质严格分开：

第一类是 USCF 官方直接提供的数据，包括最新 NAV、总资产净值、流通份额、申赎份额、买卖报价、市场价格、溢折价、当前持仓，以及最近五年的每日 NAV 和总资产净值。这些字段不做主观修正，按官方值保存。

第二类是可以按公开合约规格精确计算的数据。WTI 的 NYMEX CL 期货和 ICE Brent 期货每手均对应 1,000 桶，因此当日期货名义桶数等于各期货合约手数乘以 1,000 桶后求和。只要官方手数正确，这部分换算就是确定值。

第三类是模型估算数据。USCF 公开接口没有提供过去五年的逐日合约持仓，所以 2026-07-23 以前的历史期货桶数变化只能由官方 NAV、官方总资产净值和官方申赎篮子规则估算。USO 的掉期也没有统一的天然“桶”单位，因此掉期桶等值同样是估算。页面必须一直保留现在位于变化矩阵下方的口径说明，不得把这两类估算宣传为官方真实桶数。

如果业务要求“过去五年每一天的期货桶数都必须是官方真实持仓，误差为零”，那么当前免费公开数据无法满足，必须另外购买或取得有授权的逐日合约持仓档案。在取得这类数据以前，可以上线当前页面，但对外口径必须是“官方 NAV/总资产历史 + 历史期货持仓模型估算”，不能写成“官方历史持仓”或“100%真实历史桶数”。

## 二、已经冻结并交付的历史数据

上线初始快照位于：

- `public/data/uso-snapshot.json`
- `public/data/bno-snapshot.json`

两只基金的数据日期均截至 2026-07-23，历史区间均为 2021-07-26 至 2026-07-23，各有 1,254 个交易日。

2026-07-24 的验收已经通过 USCF 当前公开接口逐条比对：

- USO 的 1,254 条 NAV、NAV 变化、NAV 变化率和总资产净值全部与官方历史接口一致；最新 7 条持仓全部与官方持仓接口一致；官方期货手数为 23,107 手，对应 23,107,000 桶。
- BNO 的 1,254 条 NAV、NAV 变化、NAV 变化率和总资产净值全部与官方历史接口一致；最新 5 条持仓全部与官方持仓接口一致；官方期货手数为 7,593 手，对应 7,593,000 桶。
- 两只基金的最新持仓日期、最新 NAV 日期和历史最后一个交易日均为 2026-07-23。
- 历史日期无重复、严格递增；所有必需数值均为有限数；当前 NAV×流通份额与官方总资产净值在官方四舍五入误差范围内一致。

技术同事不需要再次人工抓取这 1,254 个交易日核验。上线前只需要执行本项目已经提供的自动校验命令。历史 JSON 必须作为版本化数据文件与代码一起提交，不能在浏览器端临时生成。

本次首发文件的 SHA-256 校验值为：

- `uso-snapshot.json`：`51cf8910462ae1d43c022f45debaf072a6a0bbd9db28d18a38f925bded2568ed`
- `bno-snapshot.json`：`7676537c8def9a6ca1712a65a01b63667ca87006ed49db67383245c952162f69`

这两个哈希只用于确认 2026-07-23 首发底稿没有被意外改动。产生下一个官方交易日后，JSON 和哈希正常变化，应以新的 Git commit 固定版本。

## 三、每日数据的官方来源

数据由 USCF 官网实际使用的 SS&C ALPS Marketing API 提供。每天运行任务时，必须先重新读取公开令牌，不能把令牌写死在代码、数据库或日志里。

公开令牌脚本：

`https://www.uscfinvestments.com/site-template/assets/javascript/api_key.php`

接口根地址：

`https://secure.alpsinc.com/MarketingAPI/api/v1/`

接口 Swagger：

`https://secure.alpsinc.com/MarketingAPI/swagger/index.html`

对 USO 和 BNO 分别请求以下三个接口：

1. 最新价格和基金规模：`dailyprice/USO`、`dailyprice/BNO`
2. 最新完整持仓：`holding/USO/full`、`holding/BNO/full`
3. 官方历史 NAV 与总资产：`historicalnav/USO/inception-today`、`historicalnav/BNO/inception-today`

请求头使用：

`Authorization: Bearer <从公开令牌脚本当次解析出的 token>`

产品和持仓的人工核对页面为：

- `https://www.uscfinvestments.com/uso`
- `https://www.uscfinvestments.com/holdings/uso`
- `https://www.uscfinvestments.com/bno`
- `https://www.uscfinvestments.com/holdings/bno`

合约规格的官方来源为：

- WTI：`https://www.cmegroup.com/markets/energy/crude-oil/light-sweet-crude.contractSpecs.html`
- Brent：`https://www.ice.com/products/219/Brent-Crude-Futures`

## 四、官方接口字段与本地字段的对应关系

`dailyprice` 返回数组，取第一条。`displaydate` 写入 `fund.asOfDate`；`nav` 写入 `current.nav`；`navchange` 和 `navpercent` 分别写入 NAV 日变化和变化率；`navtotal` 写入总资产净值；`so` 写入流通份额；`cr` 写入当日申购或赎回份额；`marketvalue` 写入官方 4 点买卖中间价；`premiumpercent` 写入溢折价；`bid` 和 `ask` 写入买价和卖价；`exchange` 与 `inceptiondate` 写入基金基本信息。

`holding/{symbol}/full` 只保留 `possessionname` 等于 `Hold` 的记录。`asofdate` 是持仓日期；`holdingtype` 是 Futures、Swap、Cash 或 Cash Equivalent 等类型；`name` 是持仓名称；`identifiertodisplay` 是页面展示代码；`shares` 是官方 Quantity；`contractprice` 是官方 Price；`marketvalue` 是官方 Market Value；`weight` 是该持仓相对基金净资产的官方权重。

`historicalnav` 的实际结构是数组中的基金代码对象。每个历史交易日的 `date`、`value`、`change`、`changepercent` 和 `navTotal` 分别写入日期、NAV、NAV 日变化、NAV 日变化率和总资产净值。

技术实现不得通过页面文字或 HTML 表格反向解析这些数值；应直接调用上述 JSON 接口。若接口字段名、结构或数据类型变化，任务必须失败并报警，不能用空值、零值或上一日值静默代替。

## 五、每日发布时间与北京时间任务安排

USCF 官方产品页说明，基金 NAV 一般在美国东部时间下午 4:00 左右计算。换算为北京时间：

- 美国夏令时期间，下午 4:00 ET 大约是北京时间次日 04:00。
- 美国冬令时期间，下午 4:00 ET 大约是北京时间次日 05:00。

这里的 04:00 或 05:00 是 NAV 的计算时间，不是公开 API 承诺的完成发布时间。USCF 没有公布接口必须在某个分钟更新的服务承诺，因此生产任务不能只凭时钟判断数据是否已更新。

建议每周二至周六北京时间 09:00 执行第一次抓取。若官方 `displaydate` 仍未进入新的美国交易日，则在 09:30、10:00、10:30、11:00、11:30 和 12:00 重试。美国休市日或周末没有新交易日时，任务应正常结束为“无新数据”，不得制造一条 0 变化记录。12:00 后仍没有新数据时报警，并保留上一版线上数据；可以在 15:00 再做一次补抓。

是否抓到新交易日必须同时满足：

1. USO 的 `dailyprice.displaydate` 等于 USO 所有有效持仓的 `asofdate`。
2. BNO 的 `dailyprice.displaydate` 等于 BNO 所有有效持仓的 `asofdate`。
3. USO 与 BNO 的最终日期相同。
4. 新日期不能早于已发布快照日期。
5. 官方历史 NAV 的最后一个日期必须等于同一只基金的最新日期。

任一条件不满足时，本次任务失败，两个基金都不更新。不能只更新 USO 或只更新 BNO。

## 六、计算规则

### 1. 当前期货桶数

只对 `holdingtype = Futures` 的记录计算：

`单个合约桶数 = 官方 Quantity × 1,000`

`当日期货桶数 = 所有期货合约桶数之和`

USO 和 BNO 都使用相同的 1,000 桶/手规格。计算必须保留整数手数，不得把价格、市场价值、现金或国债加入期货桶数。

### 2. 官方期货持仓日变化

从 2026-07-23 开始，每次成功刷新都把当日官方合约级持仓永久追加到 `officialHistory.rows`，以日期作为唯一键。

只有当“上一条官方快照的日期”正好也是官方历史 NAV 中的上一个交易日时，才计算：

`当日期货持仓变化 = 当日期货桶数 − 上一交易日期货桶数`

这项变化能够捕捉申赎、展期和基金主动调仓。若中间漏抓一个交易日，不能用两端官方桶数相减后把跨多日差额记到最后一天；漏抓区间继续使用下述模型估算，并在内部把 `changeBasis` 标为 `estimated-gap`。

### 3. 历史期货持仓模型

2026-07-23 以前没有免费的官方逐日持仓档案，按以下固定规则估算：

先计算每日原始流通份额：

`原始流通份额 = 官方总资产净值 ÷ 官方 NAV`

由于官方历史 NAV 只保留到小数点后两位，原始流通份额会存在小的四舍五入误差。以冻结日官方流通份额为锚，按官方最小申赎篮子取整：

`估算流通份额 = 锚定流通份额 + round((原始流通份额 − 锚定流通份额) ÷ 申赎篮子份额) × 申赎篮子份额`

USO 的申赎篮子为 100,000 份，BNO 的申赎篮子为 50,000 份。

再计算模型基准：

`每份期货桶数 = 最近一个已保存官方快照的期货桶数 ÷ 该快照官方流通份额`

当前历史冻结基准为 2026-07-23：

- USO 每份期货桶数为 1.39001154 桶。
- BNO 每份期货桶数为 0.58633205 桶。

每日模型变化为：

`估算期货桶数变化 = 当日估算流通份额变化 × 每份期货桶数`

历史模型的作用是把油价上涨或下跌造成的总资产变化剔除，主要识别申赎造成的份额变化。它不能还原过去的展期、合约月份变化、主动调仓和掉期变化，所以必须继续标注为模型估算。已经发布的估算历史必须冻结，后续任务不得因新的基准比例而重算过去数值。

### 4. USO 掉期桶等值

掉期没有统一的官方桶数量字段，按美元名义敞口折算：

`当日期货参考价 = 期货名义市值合计 ÷ 当日期货桶数`

这个参考价等价于基金当日各期货持仓按桶数加权的价格。

`单笔掉期桶等值估算 = 官方掉期 Market Value ÷ 当日期货参考价`

`USO 当前名义原油敞口 = 官方期货桶数 + 掉期桶等值估算`

掉期桶数必须显示“约”或“估算”，不能写成官方合约桶数。掉期估算不参与官方期货持仓日变化。BNO 当前没有掉期，因此当前 BNO 桶数完全来自期货手数。

### 5. 资产敞口率

`资产敞口率 = 所有 Futures 和 Swap 的官方 weight 之和`

同时用“期货和掉期 Market Value 合计 ÷ 官方总资产净值”做误差校验。由于官方 weight 有显示精度，二者允许存在很小的四舍五入差异。资产敞口率可以超过 100%，因为期货名义市值与抵押资产会同时存在，这不代表基金净资产超过实际规模。

### 6. 按美元视图

按美元展示的是 USCF 官方总资产净值的相邻交易日差：

`总资产变化 = 当日官方 Net Assets − 上一交易日官方 Net Assets`

它包含油价变化、申赎、掉期或抵押品收益和费用，不等于净申购资金流。页面现有说明不得删除或改写为“资金流入/流出”。

## 七、每日更新需要修改的文件和发布步骤

每日任务的唯一数据输出是：

- `public/data/uso-snapshot.json`
- `public/data/bno-snapshot.json`

页面在构建时直接导入这两个 JSON，因此仅修改服务器上的 JSON 而不重新构建，线上页面不会更新。每次产生新交易日后必须完成一次完整构建和部署。

推荐顺序如下：

1. 在单实例任务中执行 `npm ci`。
2. 执行 `npm run data:refresh`。
3. 如果输出 `No new USCF data`，本次正常结束，不提交、不构建、不发布。
4. 如果生成新快照，执行 `npm run data:verify:official`，再次与当日官方接口逐条对账。
5. 执行 `npm test`。
6. 执行 `npm run lint`。
7. 只有以上命令全部通过，才把两个 JSON 和代码作为同一个版本提交。
8. 构建并部署该版本。
9. 部署完成后检查线上 HTTP 200、USO/BNO 日期、USO/BNO 切换、按桶/按美元切换，以及页面显示日期是否等于快照日期。

刷新脚本已经实现以下保护：

- 两只基金先全部抓取并校验，再写入文件。
- 两只基金日期不一致时拒绝写入。
- 官方日期早于线上快照时拒绝写入。
- 持仓日期与 NAV 日期不一致时拒绝写入。
- 历史日期重复、乱序、缺少末日时拒绝写入。
- 先写临时文件并完成 JSON 解析，再替换正式文件。
- 同一交易日且内容没有变化时不重写文件。
- 已发布历史估算和既有官方归档不会被清空或回算。
- 令牌只存在于当次进程内，不写入数据文件。

生产调度还需要设置同一时间只允许一个刷新任务运行。若采用 GitHub Actions，应给数据刷新工作流设置独立的 `concurrency` 组，不能让两次任务同时写归档。

## 八、失败、报警与回滚

以下情况均为阻断发布的严重错误：

- 令牌脚本无法解析或任一接口不是 HTTP 200。
- API 返回结构变化、必需字段为空或数字不是有限数。
- USO 与 BNO 日期不同。
- 同一基金的持仓日期、最新价格日期和历史最后日期不同。
- 新日期早于当前线上日期。
- 历史日期重复或不是严格递增。
- 期货 Quantity 不是整数手数。
- 期货 Market Value 与 `Quantity × 1,000 × Contract Price` 明显不一致。
- `NAV × Shares Outstanding` 与 Net Assets 的差异超过 0.1%。
- 官方自动对账、测试、构建或线上健康检查任一失败。

发生错误时，线上继续保留上一版，不写入 0、不复制上一日为新日期、不发布半套文件。报警信息至少包含基金代码、接口、官方日期、现有日期和失败规则。

每次发布使用不可变版本号或 Git commit。若线上检查失败，回滚到上一完整版本；不能单独回滚一只基金的数据。

## 九、上线验收标准

上线前必须全部满足：

- `npm run data:verify:official` 通过。
- `npm test` 通过。
- `npm run lint` 通过。
- 生产构建通过。
- USO 与 BNO 最新日期一致。
- USO 与 BNO 各 1,254 条冻结历史仍然存在，日期无重复、严格递增。
- USO 23,107 手期货对应 23,107,000 桶；BNO 7,593 手期货对应 7,593,000 桶。
- 页面按桶默认打开，红色表示增加，绿色表示减少，零值不显示为增加或减少。
- 按美元切换后单位为亿美元，含义为总资产净值变化，不写成资金流。
- WTI/Brent、USO/BNO 切换正确，数据不会交叉。
- 历史估算说明只在变化矩阵下方集中展示，单日列表不重复占位。
- 当前 USO 掉期仍明确标注“估算”或“约”。
- 页面明确写明桶等值不是实物库存。
- 移动端和桌面端没有遮挡、溢出、错位，所有交互可用。
- 线上首页和静态资源均返回 HTTP 200。

## 十、上线结论与必须保留的风险边界

截至 2026-07-24，官方原始数据、当前持仓、当前期货桶数及页面计算已经完成自动对账。当前冻结 JSON 可以直接作为首发历史底稿使用。

唯一不能被称为“100%官方真实值”的是过去五年的期货桶数变化和 USO 掉期桶等值。它们是采用固定、可复核、已公开说明的模型估算。这个限制来自官方免费数据本身缺少逐日历史合约持仓，不是代码计算错误。

因此，本项目达到上线标准的前提是：产品对外继续准确区分“官方数据”“合约规格精确换算”和“模型估算”，并且每日任务严格保存新的官方合约级快照。只要每天不漏抓，2026-07-23 之后相邻交易日的期货桶数变化将逐日累积为官方真实合约手数差，历史估算区间不会继续扩大。
